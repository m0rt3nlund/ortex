[net]
# Cargo's built-in libgit2 SSH transport doesn't use ssh-agent the way the
# git CLI does, and fails to authenticate against private git dependencies
# (e.g. ex_mas_ai) even when `git fetch` works fine on the same machine.
# Shelling out to the system git CLI reuses whatever SSH setup already works.
git-fetch-with-cli = true
